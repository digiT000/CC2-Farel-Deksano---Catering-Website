export interface MenuProps {
  image: string;
  menuName: string;
  description: string;
}

export interface PackageProps {
  thumbnail: string;
  title: string;
  description: string;
  totalMenu: number;
}
